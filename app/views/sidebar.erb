     <aside>
          <div id="sidebar" class="nav-collapse ">
              <!-- sidebar menu start-->
              <ul class="sidebar-menu" id="nav-accordion">
              
                  <p class="centered"><a href="/users/<%= session[:user_id] %>"><img src="<%= current_user.avatar %>" class="img-circle" width="100"></a></p>
                  <h5 class="centered">Welcome, <%= username_href(session[:user_id]) %></h5>
                    
                  <li class="mt">
                      <a href="/users/<%= session[:user_id] %>">
                          <i><h1>Profile</h1></i>  
                      </a>
                  </li>

                  <li class="mt">
                      <h1>4.7</h1>
                      <i class="li_star">Influencer Score</i>
                  </li>

                  <li class="mt">
                      <i class="li_tag" style="color: white; font-size: 20px;">Used Tags</i><br/>
                        <% @tags = current_user.tags %>
                        <i style="color:white; text-align:center"><%= erb :'tags/index' %></i>
                  </li>

              </ul>
              <!-- sidebar menu end-->
          </div>
      </aside>