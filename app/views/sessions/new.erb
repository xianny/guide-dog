<!-- Yields directly from layout.erb, so its structure is equivalent to activities/index
  Login screen
  takes @user as a variable (defaults to previous login attempt or User.new)
    Need to collect parameters 
        :username
        :password
    and POST to '/sessions' -->